package com.auditlog.project.config;

import com.auditlog.project.locale.ETypeMode;

public class ConfigTypeMode {

	// ---------you can choose between ETypeMode.dev and ETypeMode.build
	public static final ETypeMode TYPE_MODE = ETypeMode.dev;
	// public static final ETypeMode TYPE_MODE = ETypeMode.build;
}
